# 주니어#<N>(<기능>) 일감 — <한 줄 제목: 무엇이 왜 잘못됐나 / 무엇을 넣나> (🔴 심각도 표시가 있으면 여기)
<!-- 인턴(intern)용이면 제목을 `인턴#<N>(<기능>)` 으로. 인턴 브리프는 「해야 할 것」에 파일 경로와 바꿀 내용(전/후)까지 적고, 완료 조건은 셸 한 줄로 판정되게 쓴다. 가드·시니어 없음. -->

## 근거

<어디서 나온 일감인지 **인용**한다 — PR 번호·docs 절·실측 로그. 워커가 "왜" 를 알아야
엉뚱한 곳을 고치지 않는다. 추측으로 쓴 문장은 넣지 않는다.>

> (원문 인용)

<이 일감의 **범위 경계**: 무엇까지이고 무엇은 다음 조각인지. 큰 판은 쪼갠다.>

## 먼저 읽어라 (추측 금지)

1. `CLAUDE.md`(있으면), 관련 설계 문서 절(행 번호까지)
2. <핵심 파일 — 함수 이름·행 번호. "어디를 보라" 가 없으면 워커는 grep 으로 헤맨다>
3. <관련 테스트 뼈대 — 재현 테스트를 본뜰 파일>
4. <같은 자리를 만지는 다른 워커가 있으면 그 사실>
5. `.claude/skills/ship/SKILL.md`(랜딩 절차, 있으면)

## 해야 할 것

1. **재현**: <고침 전 빨강인 테스트를 먼저>
2. **고쳐라**: <후보가 여럿이면 나열하고 "근거를 적어라". 관문은 문제가 실제로 생기는 한 곳에>
3. <부수 조건 — 로컬 경로 불변, 기존 테스트 그대로 통과 등>
4. **테스트**: <무엇을 단언하나. "가드 빼면 빨강" 을 **각 가드마다**>
5. <화면이 있으면> 폰 확인: 별도 오케·웹·브랜치 DB 로 띄우고 `http://<머신>.tail611bf8.ts.net:<포트>` 를 보고에. **끝나면 내려라.**

## 완료 조건 (추가)

- 브랜치 `<종류>/<이름>`
- <docs 에 남길 절 이름>
- 파일 경계: <이 워커가 만져도 되는 곳 / 다른 워커가 쓰는 곳>
- ⚠️ 가드 실험을 되돌릴 때 **통째로 치환하지 마라** — `git checkout -- <파일>` 뒤 의도한 줄만 다시 고쳐라. PR 본문에 `git diff origin/main --stat` **실제 출력**(`$(…)` 문자열이 아니라).

## 공통 규칙

<!-- 여기에 쓰지 마라. launch-worker.sh 가 `common-rules.md`(정본)를 자리표시자까지 채워 TASK.md 끝에 붙인다.
     프로젝트 고유값은 `<저장소>/.orca/project.env`(--project-env 로 바꿀 수 있다), 보고 Run 은 `--run <RUN_ID>`. -->
