# 한국어 작성

한국어로 답변할 때는 의미가 있는 문장 성분과 조사·어미를 생략하지 않고, 서술어와 종결어미가 있는 완전한 문장을 씁니다. 널리 쓰이는 한국어 기술 용어가 있으면 사용하고, 그렇지 않으면 원어를 유지합니다. 불필요한 비유와 엠대시는 피합니다. 인용, 코드, 코드 주석과 프로젝트 관례를 따르는 커밋 메시지·로그에는 이 규칙을 적용하지 않습니다.

문서 작성·교정, 이슈·PR 본문, 사용자에게 전달할 장문의 보고처럼 한국어 문체의 완성도가 중요한 작업에서는 [fluent-korean 스킬](plugins/oh-my-teams/skills/fluent-korean/SKILL.md)을 전체 읽고 지침과 예시를 적용합니다. 사용자가 해당 스킬을 직접 요청한 경우에도 읽습니다. 같은 대화에서 이미 전체를 읽었다면 다시 읽지 않습니다.

# Git 브랜치와 병합

기능 개발은 `feat/<주제>`, 버그 수정은 `fix/<주제>` 브랜치에서 진행한 뒤 `main`에 병합합니다. 문서, 리팩터링, 테스트와 유지보수 작업에는 각각 `docs/`, `refactor/`, `test/`, `chore/` 접두사를 사용합니다. 긴급 복구가 아니라면 `main`에서 직접 개발 커밋을 만들지 않습니다.

# 버전 정책

호환 가능한 기능 추가와 버그 수정은 `2.x.x` 범위에서 버전을 올립니다. 주 버전은 제품 계약을 크게 바꾸는 비호환 변경이 있을 때에만 사용자의 명시적인 결정에 따라 변경합니다.

# 코드 품질과 포맷

활성 `.mjs` 파일에는 모듈 JSDoc을 작성하고, 공개 export에는 인접 JSDoc과 `@param`·`@returns`를 붙입니다. 실행문은 180자를 넘기지 않습니다. 포맷은 Prettier가 결정하므로 직접 맞추려고 하지 말고 `npm run format`을 실행합니다.

작업을 마치면 `npm run sync`로 공통 정보를 맞춘 뒤 `npm run lint`와 `npm test`를 실행해 통과를 확인하고 커밋합니다. 포맷 검사는 devDependency로 설치한 Prettier를 사용하므로, 새로 클론한 작업 트리에서는 `npm ci`를 먼저 실행합니다. 두 명령은 GitHub Actions의 `CI` 워크플로에서도 실행되기 때문에, 병합하기 전에 그 결과를 확인합니다.

검사 기준의 상세한 내용은 [코드 품질·문서화 기준](docs/CODE_QUALITY.md)에 정리되어 있습니다.

# 여러 파일이 되풀이하는 공통 정보

버전과 감사 수치처럼 여러 파일이 같은 값을 적어야 하는 정보는 `scripts/metadata.mjs`가 한 곳에서 관리합니다. **이 값들을 파일마다 직접 고치지 말고 정본만 바꾼 뒤 `npm run sync`를 실행합니다.**

| 정보 | 정본 | 따라가는 곳 |
|---|---|---|
| 버전 | `package.json`의 `version` | `.claude-plugin/marketplace.json`, Claude·Codex의 `plugin.json` |
| 활성 모듈·공개 export·태그 수 | `npm run quality`의 감사 결과 | `docs/PLAN_STATUS.md`, `docs/SAFETY_AUDIT.md`, `docs/CODE_QUALITY.md` |
| 테스트 개수 | `tests/*.test.mjs`의 최상위 `test(` 선언 | `docs/PLAN_STATUS.md`, `docs/SAFETY_AUDIT.md` |
| eval 시나리오 수 | `evals/organization/scenarios.json` | `docs/PLAN_STATUS.md`, `docs/SAFETY_AUDIT.md` |

`npm run sync:check`는 고치지 않고 어긋난 곳만 보고하며, `tests/repository-metadata.test.mjs`가 같은 검사를 실행하므로 동기화를 잊으면 테스트가 실패합니다. Codex manifest만 `+codex.<타임스탬프>` 형태의 빌드 메타데이터를 덧붙이는데, 기본 버전이 바뀔 때에만 타임스탬프를 새로 만듭니다. 버전을 바꾼 뒤에는 `npm install`을 실행해서 `package-lock.json`에 기록된 버전까지 함께 동기화합니다.

값을 되풀이하는 문장을 새로 쓰거나 기존 문장의 표현을 바꿀 때에는 `metadataTargets`의 해당 정규식도 함께 고쳐야 합니다. 문장을 찾지 못하면 `sync`가 그 값을 지어내지 않고 `missing`으로 보고합니다.

# 조직 및 역할 규칙

- **이사만 close·병합한다**: 워크플로의 최종 결과물을 확인하고 병합, 이슈 닫기 등의 종료 처리는 이사(Director) 역할만 수행합니다.
- **PM 이하는 사용자에게 직접 묻지 않는다**: PM, PL, Senior, Junior 등의 역할은 사용자에게 직접 질문하거나 보고하지 않으며, 결정이 필요한 사항은 반드시 상위 보고 대상에게 올려서 처리합니다.
- **무거운 작업 전 자원 슬롯**: 테스트, 빌드, 무거운 worker 실행 등 자원을 많이 소모하는 작업 전에는 자원 슬롯을 확보하여 실행하며, 작업이 끝난 후에는 반드시 해제합니다.

# 아키텍처 규칙

- **Orca의 감독 기능을 재구현하지 않는다**: 스킬 패키지는 업무 계약과 gate를 담당하고, 실행 소유권·프로세스·회수는 Orca를 사용합니다. (단, `docs/plan/headless-runtime.md`가 명시적으로 예외를 선언한 헤드리스 실행 경로는 예외로 합니다.)
- **어댑터를 거치지 않는 직접 호출 금지**: 플랫폼별 실행 파일 선택, JSON 호출, 디렉터리 생성 등의 런타임 환경 접근은 반드시 지정된 어댑터를 거쳐야 합니다.
- **스킬 문서에 런타임 로직 중복 금지**: 런타임에서 강제하는 값이나 검사 로직을 스킬 문서에 중복해서 선언하지 않아야 합니다.
- **사용자 설정 파일을 쓰지 않는다**: 플러그인은 사용자 설정 파일에 직접 쓰기 작업을 수행하지 않습니다.
- **임시 산출물을 워크트리에 남기지 않는다**: 조사용 스크립트 등 임시 산출물은 작업 워크트리 밖의 OS 임시 디렉터리에 작성하며 추적되지 않는 파일을 워크트리에 남기지 않습니다.
