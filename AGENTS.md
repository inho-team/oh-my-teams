# 한국어 작성

한국어로 답변할 때는 의미가 있는 문장 성분과 조사·어미를 생략하지 않고, 서술어와 종결어미가 있는 완전한 문장을 씁니다. 널리 쓰이는 한국어 기술 용어가 있으면 사용하고, 그렇지 않으면 원어를 유지합니다. 불필요한 비유와 엠대시는 피합니다. 인용, 코드, 코드 주석과 프로젝트 관례를 따르는 커밋 메시지·로그에는 이 규칙을 적용하지 않습니다.

문서 작성·교정, 이슈·PR 본문, 사용자에게 전달할 장문의 보고처럼 한국어 문체의 완성도가 중요한 작업에서는 [fluent-korean 스킬](plugins/oh-my-teams/skills/fluent-korean/SKILL.md)을 전체 읽고 지침과 예시를 적용합니다. 사용자가 해당 스킬을 직접 요청한 경우에도 읽습니다. 같은 대화에서 이미 전체를 읽었다면 다시 읽지 않습니다.

# Git 브랜치와 병합

기능 개발은 `feat/<주제>`, 버그 수정은 `fix/<주제>` 브랜치에서 진행한 뒤 `main`에 병합합니다. 문서, 리팩터링, 테스트와 유지보수 작업에는 각각 `docs/`, `refactor/`, `test/`, `chore/` 접두사를 사용합니다. 긴급 복구가 아니라면 `main`에서 직접 개발 커밋을 만들지 않습니다.

# 버전 정책

호환 가능한 기능 추가와 버그 수정은 `1.x.x` 범위에서 버전을 올립니다. 주 버전은 제품 계약을 크게 바꾸는 비호환 변경이 있을 때에만 사용자의 명시적인 결정에 따라 변경합니다.
